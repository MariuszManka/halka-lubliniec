import { defineType } from "sanity";

// Schemat jest przygotowany na później, ale nie jest jeszcze pokazany w Studio
// ani nie tworzymy dla niego dokumentu w datasecie.
export const historyPage = defineType({
  name: "historyPage",
  title: "Historia",
  type: "document",
  fields: [],
});
